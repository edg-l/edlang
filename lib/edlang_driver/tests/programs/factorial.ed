mod Main {
    pub fn main() -> i32 {
        let b: i32 = factorial(4);
        return b;
    }

    fn factorial(n: i32) -> i32 {
        if n == 0 {
            return n;
        } else {
            return n * factorial(n - 1);
        }
    }
}
